<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.heh.std.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/newicon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.PlcLiquidActivity"></activity>
        <activity android:name=".activities.PlcPillsActivity" />
        <activity android:name=".activities.forms.AddPlcActivity" />
        <activity android:name=".activities.PlcManagementActivity" />
        <activity android:name=".activities.AdminUserActivity" />
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.forms.ModProfileActivity" />
        <activity android:name=".activities.forms.ChgPasswordActivity" />
        <activity android:name=".activities.forms.RegisterActivity" />
        <activity android:name=".activities.forms.LoginActivity" />
        <activity android:name=".activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>